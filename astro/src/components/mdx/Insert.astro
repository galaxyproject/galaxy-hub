---
/**
 * Insert MDX wrapper
 * Re-exports the main Insert component for use in MDX content
 */
import InsertBase from '../Insert.astro';

interface Props {
  name: string;
}

const { name } = Astro.props;
---

<InsertBase name={name} />
