---
import Layout from '../../layouts/BaseLayout.astro';
import MarkdownEmbed from '../../components/MarkdownEmbed.vue';
---

<Layout title="MarkdownEmbed Component Test">
  <div class="container my-5">
    <h1>MarkdownEmbed Component Test</h1>
    
    <h2>Remote Markdown File</h2>
    <p>This tests loading markdown from a remote URL.</p>
    <MarkdownEmbed 
      client:load 
      href="https://raw.githubusercontent.com/galaxyproject/galaxy/dev/README.md" 
    />
    
    <h2>Local Markdown File</h2>
    <p>This tests loading markdown from a local file.</p>
    <MarkdownEmbed 
      client:load 
      href="/test-content/sample.md" 
    />
    
    <h2>Direct Markdown Content</h2>
    <p>This tests passing markdown content directly.</p>
    <MarkdownEmbed 
      client:load 
      content={`# Direct Markdown

This is **bold** text and this is *italic* text.

## Features

- Bullet point 1
- Bullet point 2
- Bullet point 3

### Code Example

\`\`\`javascript
function hello() {
  console.log("Hello, Galaxy!");
}
\`\`\`

> This is a blockquote with some important information.

[Link to Galaxy](https://galaxyproject.org)`}
    />
    
    <h2>Error Handling</h2>
    <p>This tests error handling with a non-existent URL.</p>
    <MarkdownEmbed 
      client:load 
      href="/does-not-exist.md" 
    />
  </div>
</Layout>